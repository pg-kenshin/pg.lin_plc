# TRITON PLC 專案

## 關於我

PLC 新手。這個專案同時是我的學習過程,不只是要把東西做出來。

- 請解釋原理,不要只給結論或指令
- 遇到我明顯搞錯觀念時直接指出來,不用客氣
- 專有名詞第一次出現時簡短說明
- WSL、Git、GitHub 對我也都是新領域

## 硬體

**PLC:** LINX TRITON PLC-APT-CM4F(AL PiTron CM4,2GB RAM / 16GB eMMC,型號 50690 001 Rev.001)

**控制軟體:** CODESYS

這是二手機,前手用 CODESYS 做控制。

目前同時有兩台在追蹤:

| | 位置 | 連接方式 | 狀態 |
|---|---|---|---|
| 第一台 | 公司 | USB 轉 LAN 直接對接電腦 | 已用終端機成功登入,不需重燒。eth1 固定 IP `192.168.1.21` |
| 第二台 | 家裡 | 電腦 LAN → HUB → TRITON | 已用終端機成功登入,不需重燒。eth1 固定 IP `192.168.3.41`  |

## 開發環境

兩台電腦協作,靠 GitHub 同步:

- **個人電腦(家)** — Windows + WSL、CODESYS、VS Code、Python、Git(Windows 與 WSL 兩邊都有)
- **筆電(外出)** — Windows、CODESYS

Git 使用 **Windows 版**(原本規劃放在 WSL,後來為了配合教學改用 Windows 版)。

Claude Code 走**官方 Claude 訂閱**登入,不使用第三方 API 供應商。

## 學習方向

短期是把 PLC 控制跑起來。往後想延伸到:

- 通訊協定的讀寫,特別是 CAN 與 RS-485
- Python + ESP32 的 IoT 開發環境

主要在 VS Code 裡編寫。

## 協作規則

**修改前先說明** — 動任何檔案之前,先講你要改什麼、為什麼。我需要看懂每一步。

**不要碰 `.git` 資料夾** — 版本控制我自己來。要執行 git 指令請先問我。

**CODESYS 專案檔是二進位格式** — Git 追蹤得到但看不到 diff、也不能合併。所以絕對避免讓兩台電腦同時改同一個檔案。有疑慮時提醒我先 pull。

**新增檔案時說明用途** — 我需要知道每個檔案為什麼存在。

**不確定就問** — 這台 PLC 是二手的,很多設定我自己也還不清楚。與其猜測,不如讓我去查或去試。

**改動 PLC 實體行為前務必確認** — 任何會影響輸出、馬達、繼電器的變更,執行前一定要先問過我。
